<configuration>
    <!-- Консольный appender для отладки -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Отправка логов в Elasticsearch -->
    <appender name="ELASTIC" class="net.logstash.logback.appender.LoggingEventAsyncDisruptorAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- Включение MDC (полезно для передачи данных о контексте заявки) -->
            <includeMdc>true</includeMdc>
            <!-- Формат времени -->
            <timeZone>UTC</timeZone>
        </encoder>
        <destination>http://localhost:9200/_bulk</destination>
    </appender>

    <!-- Логгеры -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="dn.mp_orders" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ELASTIC"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ELASTIC"/>
    </root>
</configuration>